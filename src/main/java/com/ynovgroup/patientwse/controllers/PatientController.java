package com.ynovgroup.patientwse.controllers;

/*import java.util.ArrayList;
import java.util.List;
import java.util.stream.Collectors;

import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;

import com.ynovgroup.patientwse.models.Patient;
*/


public class PatientController {

	/*	List<Patient> patients = new ArrayList<Patient>();
		{
			patients.add(new Patient(123, "Sonia", 63));
			patients.add(new Patient(234, "Ndeya", 24));
			patients.add(new Patient(345, "Hamid", 34));
			patients.add(new Patient(456, "Bouba", 4));
		}


		@RequestMapping(value = "/getPatients")
		public List<Patient> getPatients() {
			return patients;
		}


		@RequestMapping(value = "/getPatientByName/{name}")
		public List<Patient> getPatientByName(@PathVariable(value = "name") String name) {
			System.out.println("Searching Patient by name : " + name);
			List<Patient> patientsByName = patients.stream().filter(x -> x.getPatientName().equalsIgnoreCase(name))
					.collect(Collectors.toList());
			System.out.println(patientsByName);
			return patientsByName;
		}
		
		
		public Patient get
*/
}
