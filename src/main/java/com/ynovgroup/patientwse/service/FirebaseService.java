package com.ynovgroup.patientwse.service;

/*import java.util.concurrent.ExecutionException;

import org.springframework.stereotype.Service;

import com.google.cloud.firestore.Firestore;
import com.google.firebase.cloud.FirestoreClient;
import com.ynovgroup.patientwse.models.Patient;
*/

//@Service
public class FirebaseService {
	/*public String savePatientDetails(Patient patient) throws InterruptedException, ExecutionException {
		Firestore dbFirestore = FirestoreClient.getFirestore();
		Apifuture<WriteResult> collectionApifuture = dbFirestore.collection("patients").document(patient.getPatientName()).set(null)         
		return collectionApiFuture.get().getUpdateTime().toString();
	}*/
}
